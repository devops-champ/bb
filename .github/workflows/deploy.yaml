# Placeholder
# Due to response-size limits, the complete enterprise workflow should be
# authored as a standalone file. This template shows the intended stage layout.

name: Liquibase PostgreSQL Review

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  version:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

  build:
    needs: version
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

  prepare-context:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

  docker-build:
    needs: prepare-context
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

  docker-save:
    needs: docker-build
    runs-on: ubuntu-latest
    steps:
      - run: echo save image

  liquibase-validate:
    needs: docker-save
    runs-on: ubuntu-latest
    steps:
      - run: echo validate

  liquibase-update:
    needs: liquibase-validate
    runs-on: ubuntu-latest
    steps:
      - run: echo update

  tag-baseline:
    needs: liquibase-update
    runs-on: ubuntu-latest
    steps:
      - run: echo tag

  compare-main:
    needs: tag-baseline
    runs-on: ubuntu-latest
    steps:
      - run: echo compare# Placeholder
# Due to response-size limits, the complete enterprise workflow should be
# authored as a standalone file. This template shows the intended stage layout.

name: Liquibase PostgreSQL Review

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  version:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

  build:
    needs: version
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

  prepare-context:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

  docker-build:
    needs: prepare-context
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

  docker-save:
    needs: docker-build
    runs-on: ubuntu-latest
    steps:
      - run: echo save image

  liquibase-validate:
    needs: docker-save
    runs-on: ubuntu-latest
    steps:
      - run: echo validate

  liquibase-update:
    needs: liquibase-validate
    runs-on: ubuntu-latest
    steps:
      - run: echo update

  tag-baseline:
    needs: liquibase-update
    runs-on: ubuntu-latest
    steps:
      - run: echo tag

  compare-main:
    needs: tag-baseline
    runs-on: ubuntu-latest
    steps:
      - run: echo compare
